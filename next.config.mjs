/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow external images if needed
  images: {
    domains: ['tbljrnluurinvuueftwx.supabase.co'],
  },
};

export default nextConfig;
